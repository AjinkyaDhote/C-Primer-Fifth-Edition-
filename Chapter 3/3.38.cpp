//In the example it would be meaningless to add two pointers
//because they are pointing at different addresses(different objects) in the memory 
//It would be valid only if they would be pointing to the same object