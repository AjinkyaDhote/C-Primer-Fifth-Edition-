If the *scores* array is defined outside a function and not initialized then all the values are set to 0
whereas if it is defined inside a function and not initialized then all the values are undefined.