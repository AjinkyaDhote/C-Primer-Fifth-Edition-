//Read Function defines its Sales_data parameter as a plain reference because the function alters/changes the object
//passed to it.

//Print Function defines its Sales_data parameter as a const reference because the function is not going to changes
//the object passed to it.