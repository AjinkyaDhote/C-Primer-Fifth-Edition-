//Encapsultation is the method of keeping the class implementation hidden from the user of the class. 
//User code cannot access hence corruupt the state of an encapsulated object. It can directly access the interface to perform all the
//functions and does not have to worry about the internal implementation.
