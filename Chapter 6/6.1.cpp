///Arguments are initializers of Parameters. Arguments are sent from the function caller whereas Parameters are requirements of the function 
///to execute. The type of each argument must match the type of parameter and also the number of arguments to a function should be same as the
///number of parameters required.